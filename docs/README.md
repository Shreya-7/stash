# docs/

How to work with the stash codebase, blog, and deployment.

This is the operational counterpart to [`decisions/`](../decisions/):

- **`decisions/`** — *why* we built it the way we did (architectural decision records, immutable once accepted).
- **`docs/`** — *how* to do things in it (guides, kept up to date as the system evolves).
